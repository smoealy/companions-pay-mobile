import React from 'react';
import { View, Text } from 'react-native';

export default function AIPlanner() {
  return (
    <View style={{ flex: 1 }}>
      <Text style={{ textAlign: 'center', marginTop: 50 }}>AI Planner (iframe embed or chat)</Text>
    </View>
  );
}
